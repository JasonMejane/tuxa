import { BehaviorParameters } from './behavior-parameters.model';
export declare class CursorTrashingParameters implements BehaviorParameters {
    threshold: number;
    timeRange: number;
    constructor(behaviorParameters?: BehaviorParameters);
}
