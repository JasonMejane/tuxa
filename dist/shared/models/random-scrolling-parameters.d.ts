import { BehaviorParameters } from './behavior-parameters.model';
export declare class RandomScrollingParameters implements BehaviorParameters {
    threshold: number;
    timeRange: number;
    constructor(behaviorParameters?: BehaviorParameters);
}
