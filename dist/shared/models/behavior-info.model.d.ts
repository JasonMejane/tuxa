export declare class BehaviorInfo {
    date: Date;
    element: HTMLElement | null;
    name: string;
    url: string;
    constructor(name: string, element: HTMLElement | null, date: Date, url: string);
}
