export class BehaviorParameters {
	public threshold: number;
	public timeRange: number;

	constructor(threshold: number, timeRange: number) {
		this.threshold = threshold;
		this.timeRange = timeRange;
	}
}
